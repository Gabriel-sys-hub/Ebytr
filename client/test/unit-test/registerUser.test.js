/* import { render, screen } from '@testing-library/react';
import Login from '../../pages/Login';

describe('LOGIN', () => {
  it('Ao renderizar, aparece a tela de login', () => {
    render(<Login/>);
    
    screen.debug();
  })
}) */